# Used by `npm run dev`. Keep API relative so Vite can proxy to nonprod.
VITE_API_BASE_URL=/api
# Proxy /api and /.auth to nonprod SWA (not local Functions).
VITE_DEV_API_PROXY=https://icy-water-0ce7d5b10.7.azurestaticapps.net
