# Public client settings only. Never put provider secrets in VITE_* variables.
VITE_API_BASE_URL=/api

# Dev-server proxy target for /api and /.auth (see vite.config.ts).
# Default in .env.development is nonprod SWA. For a local Functions host:
# VITE_DEV_API_PROXY=http://127.0.0.1:7071
VITE_DEV_API_PROXY=https://icy-water-0ce7d5b10.7.azurestaticapps.net
